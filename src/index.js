import React from "react";
import ReactDOM from "react-dom";

import Accordion from "./accordion/App";
ReactDOM.render(
  <React.StrictMode>
    <Accordion />
  </React.StrictMode>,
  document.getElementById("root")
);
